// The project's canonical source repository, shared by every link to it (the
// status bar's GitHub icon, the privacy page's code and issues links) so they
// can't drift apart.
export const REPO_URL = 'https://github.com/scsp-op/BLACKOUT'
export const ISSUES_URL = `${REPO_URL}/issues`
